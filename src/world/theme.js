// ================= ТЕМА СВІТУ (токени художнього напряму) =================
// Один об'єкт, який визначає, як виглядає Оболонь. Міняючи токени — міняємо стиль
// усього світу (без правок рендеру). Значення нижче — робоча «тепла» база;
// фінальний напрям приходить з арт-бібліі.

export const THEME = {
  // підкладка / природа
  ground: '#efe9dd', // земля-папір
  green: '#d7e3c4', // парки, двори, зелень
  water: '#bcd8e6',
  waterEdge: '#a6c6d8',

  // дороги
  road: '#ffffff', // проїзна частина головних
  roadMinor: '#f6f1e7', // двори/проїзди
  roadEdge: '#ded5c6', // облямівка (дає «вирізаність» дороги)
  rail: '#c3bbae',

  // будівлі (псевдо-об'єм)
  building: '#e8dfd0',
  buildingSide: '#d3c8b6', // бік/тінь
  buildingEdge: '#cfc4b1',
  buildingLift: 0.38, // пікселів «висоти» на поверх
  buildingShadowDir: [0.55, 0.83], // напрям падіння тіні (нормалізований)

  // листівка (шер-картка)
  cardBg: '#f6f1e6',
  cardInk: '#26221a',
  cardInkSoft: 'rgba(38,34,26,.62)',
  cardChip: 'rgba(0,0,0,.05)',
  cardEdge: 'rgba(0,0,0,.10)',
  cardVignette: 'rgba(20,18,12,.28)',
};

/** Застосувати інший набір токенів (наприклад, нічний). */
export function setTheme(patch) {
  Object.assign(THEME, patch || {});
}
